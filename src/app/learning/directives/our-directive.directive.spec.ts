import { OurDirectiveDirective } from './our-directive.directive';

describe('OurDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OurDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
