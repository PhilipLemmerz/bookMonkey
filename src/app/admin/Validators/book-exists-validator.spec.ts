import { BookExistsValidator } from './book-exists-validator';

describe('BookExistsValidator', () => {
  it('should create an instance', () => {
    expect(new BookExistsValidator()).toBeTruthy();
  });
});
